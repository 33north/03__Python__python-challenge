# Importing modules/packagesimport csvimport osimport pprint as pp# Define the csvpath variable using os.path.join methodcsvpath = os.path.join("resources", "budget_data.csv")# Use context manager to open and read the budget_data.csv filewith open(csvpath, "r") as budget_data:    # Reads the data from the budget_data.csv via the csv.reader method into csv_reader variable    csv_reader = csv.reader(budget_data, delimiter = ",")    csv_header = next(csv_reader)    print(csv_header)        # Creates empty list and the following variables:    budget_data_list = []    total_months = 0    total = 0    diff_amt = 0    avg_change = 0    greatest_increase_profit = 0    greatest_decrease_profit = 0        # This for loop reads each row of csv_reader object and appends to budget_data_list list    for row in csv_reader:        print(row)        total_months += 1        total = total + int(row[1])        # budget_data_list.append(row)            # total_months = len(budget_data_list)    # for i in range(0, len(budget_data_list)):    #     pp.pprint(budget_data_list[i])    #     total += int(budget_data_list[i][1])    #     if i == 0:        #         diff_amt = int(budget_data_list[i+1][1]) - int(budget_data_list[i][1])    #     elif i == len(budget_data_list):    #         break            #     avg_change += diff_amt    #     avg_change /= total_months            print("""Financial Analysis\n----------------------------""")    print(f"Total Months: {total_months}")    print(f"Total: ${total}")    print(f"Average Change: {avg_change}")    print(f"Greatest Increase In Profits: {greatest_increase_profit}")    print(f"Greatest Decrease In Profits: {greatest_decrease_profit}")    